// Jai Shree Babosa!

#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define inf INT_MAX
const int mod = 1e9 + 7;

void solve()
{
    ll x, y;
    cin >> x >> y;

    if (x == y)
        cout << x << endl;
    else if(x > y)
        cout << x + y << endl;
    else
    {
        
    }
}
int main()
{
    int t;
    cin >> t;
    while (t--)
        solve();
    return 0;
}