#include <bits/stdc++.h>
using namespace std;

class TreeNode
{
private:
    TreeNode *left;
    TreeNode *right;
    int val;

public:
    TreeNode(int data)
    {
        left = NULL, right = NULL, val = data;
    }
};

int main()
{
    // int n;
    // cin >> n;

    // int array[n];
    // int minimum = INT_MAX, maximum = INT_MIN;
    // for (int i = 0; i < n; i++)
    // {
    //     cin >> array[i];
    //     minimum = min(minimum, array[i]);
    //     maximum = max(maximum, array[i]);
    // }

    // double average = (minimum + maximum) / 2.0;
    // cout << average;

    TreeNode *root = new TreeNode(0);
    root->left =

        return 0;
}