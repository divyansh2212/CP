// Jai Shree Babosa!

#include <bits/stdc++.h>
using namespace std;
#define ll long long

int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        int n;
        cin >> n;

        vector<ll> l(n);
        for (int i = 0; i < n; i++)
            cin >> l[i];

        vector<ll> r(n);
        for (int i = 0; i < n; i++)
            cin >> r[i];

        if(n == 1)
        cout << 0 << endl;
        else
        {
            
        }

    }
    return 0;
}